global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: mangascrolls-server
    metrics_path: /metrics
    static_configs:
      - targets: ['server:4000']
        labels:
          service: server
          compose_project: mangascrolls
